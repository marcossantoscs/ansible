<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Relatório de Usuários e Grupos</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .tabela { float: left; margin: 20px; border-collapse: collapse; }
        .tabela th, .tabela td { border: 1px solid #000; padding: 5px; text-align: left; }
        .tabela th { background-color: #f2f2f2; }
    </style>
</head>
<body>
    {% for servidor in servidores %}
        <table class="tabela">
            <thead>
                <tr><th colspan="2">{{ servidor.servidor | upper }}</th></tr>
                <tr><th>Usuarios</th><th>Grupos</th></tr>
            </thead>
            <tbody>
                {% for u, g in servidor.usuarios|zip(servidor.grupos) %}
                <tr>
                    <td>{{ u }}</td>
                    <td>{{ g }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    {% endfor %}
</body>
</html>
